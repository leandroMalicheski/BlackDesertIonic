export class Horse {
	constructor(public id: string, public tier: string, public code: string, public vel:number, public acc: number, public agi: number, public horseBreak: number, public icon: string){}
}